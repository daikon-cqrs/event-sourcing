{
    "name": "daikon/cqrs",
    "type": "library",
    "description": "CQRS and event-sourcing library for PHP7.",
    "keywords": [ "cqrs", "event-sourcing", "immutable" ],
    "homepage": "https://github.com/daikon-cqrs/cqrs",
    "license": "MPL-2.0",
    "prefer-stable": true,
    "minimum-stability": "dev",
    "require": {
        "php": ">=7.1",
        "daikon/entity": "master@dev",
        "daikon/message-bus": "master@dev",
        "daikon/data-structure": "master@dev"
    },
    "require-dev": {
        "phpunit/phpunit": "^6.2",
        "satooshi/php-coveralls": "master@dev",
        "squizlabs/php_codesniffer": "^3.0"
    },
    "autoload": {
        "psr-4": { "Daikon\\Cqrs\\" : "src" }
    },
    "autoload-dev": {
        "psr-4": { "Daikon\\Tests\\Cqrs\\": "tests" }
    },
    "scripts": {
        "build": [
            "@test",
            "@code-sniffer",
            "@validate-json"
        ],
        "test": "phpunit",
        "code-sniffer": "phpcs --extensions=php --standard=psr2 src tests",
        "code-sniffer-autofix": "phpcbf --standard=psr2 -d tabWidth=4 src",
        "validate-json": "composer validate --no-check-all --no-check-lock",
        "validate-deps": "composer validate --with-dependencies --strict"
    }
}
